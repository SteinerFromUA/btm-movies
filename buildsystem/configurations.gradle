ext.versions = [
        'root'       : [
                kotlin                       : "1.3.72",
                gradleDist                   : "6.5",
                androidPlugin                : "4.0.0",
                googleServicesPlugin         : "4.3.3",
                versionsPlugin               : "0.28.0",
                ktLintPlugin                 : "0.37.1",
                spotlessPlugin               : "4.3.0",
                firebaseAppDistributionPlugin: "2.0.0",
                firebaseCrashlyticsPlugin    : "2.1.1",
                ossLicensesPlugin            : "0.10.2",
        ],

        'buildConfig': [
                buildTools: "30.0.0",
                minSdk    : 21,
                targetSdk : 29,
                compileSdk: 30,
        ],

        'app'        : [
                'major' : 0,
                'minor' : 0,
                'patch' : 2,
                'build' : 1,
                'suffix': "",
        ],
]

// Manifest version information
ext.versions.app['name'] = "${versions.app.major}.${versions.app.minor}.${versions.app.patch}"
if (versions.app.build > 0) ext.versions.app['name'] += "-${versions.app.build}"
if (!(versions.app.suffix as String).isEmpty()) ext.versions.app['name'] += "-${versions.app.suffix}"

ext.versions.app['code'] = versions.app.major * 1000000 + versions.app.minor * 10000 + versions.app.patch * 100 + versions.app.build

ext.endpoints = []

ext.testData = []

ext.otherData = []

ext.signConfig = [
        'debug': [],
]
